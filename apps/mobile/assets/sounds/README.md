Este archivo es un placeholder para el sonido de match.
El archivo real (match.mp3) debe ser un audio breve de éxito (~0.5–1s).
MatchPayoff lo carga dinámicamente con expo-av y falla silenciosamente si no existe.
